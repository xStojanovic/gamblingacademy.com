'use client';
import {useState} from 'react';import {useLearning} from '@/modules/learner/components/LearningStore';
export default function ResourceCard({resource,compact=false}){const {data,toggleResource}=useLearning();const [preview,setPreview]=useState(false);const saved=data.savedResources.includes(resource.slug);return <><article className={`card resource-card ${compact?'resource-compact':''}`}><div className="course-topline"><span className="pill">{resource.category}</span><span className="file-badge">{resource.format}</span></div><h3>{resource.title}</h3><p className="muted">{resource.description}</p><div className="resource-actions"><button className="button button-secondary button-small" type="button" onClick={()=>setPreview(true)}>Preview</button><button className={`button button-small ${saved?'button-secondary':''}`} type="button" onClick={()=>toggleResource(resource.slug)}>{saved?'Saved ✓':'Save resource'}</button></div></article>{preview&&<div className="modal-backdrop"><div className="modal-card"><div className="row-between"><div><span className="pill">{resource.category}</span><h2 style={{marginTop:16}}>{resource.title}</h2></div><button className="modal-close" onClick={()=>setPreview(false)}>×</button></div><p className="lead">{resource.description}</p><div className="resource-preview"><div className="eyebrow">Template preview</div><h3>Purpose</h3><p>This resource provides a reusable structure for {resource.title.toLowerCase()} work. Adapt fields and approval steps to your organization’s approved processes.</p><h3>Suggested sections</h3><ul><li>Context and objective</li><li>Owner and stakeholders</li><li>Status, risks or decisions</li><li>Actions and due dates</li><li>Review / approval record</li></ul></div><div className="row"><button className="button" onClick={()=>toggleResource(resource.slug)}>{saved?'Remove from saved':'Save to library'}</button><button className="button button-secondary" onClick={()=>window.print()}>Print / Export</button></div></div></div>}</>}
