'use client';
import {useState} from 'react';import {useAdmin} from '@/modules/admin/components/AdminStore';
export default function ApiKeyManager(){const {data,addItem,removeItem,log}=useAdmin();const [name,setName]=useState('');function create(){if(!name)return;const prefix=`oa_live_${Math.random().toString(36).slice(2,8).toUpperCase()}`;addItem('apiKeys',{name,prefix:`${prefix}••••`,scope:'reports:read',created:'Now',lastUsed:'Never',status:'Active'});log('Created API key',name);setName('')}return <><div className="dashboard-panel"><div className="row-between"><div><h3>Platform API keys</h3><p className="muted small">Keys shown here are demo prefixes only. Production secrets must be created server-side and hashed at rest.</p></div><div className="row"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Integration name"/><button className="button button-small" onClick={create}>Create key</button></div></div></div><div className="dashboard-panel table-wrap"><table className="table"><thead><tr><th>Name</th><th>Key prefix</th><th>Scope</th><th>Created</th><th>Last used</th><th></th></tr></thead><tbody>{data.apiKeys.map(k=><tr key={k.id}><td><b>{k.name}</b></td><td><code>{k.prefix}</code></td><td>{k.scope}</td><td>{k.created}</td><td>{k.lastUsed}</td><td><button className="ghost-button danger" onClick={()=>removeItem('apiKeys',k.id)}>Revoke</button></td></tr>)}</tbody></table></div></>}
