'use client';
import {useLearning} from '@/modules/learner/components/LearningStore';
export default function NotificationsCenter(){const {data,markNotification,markAllNotifications}=useLearning();const unread=data.notifications.filter(n=>!n.read).length;return <div className="dashboard-panel"><div className="row-between"><div><h3>Notifications</h3><p className="muted small">{unread} unread update{unread===1?'':'s'}.</p></div><button className="ghost-button" onClick={markAllNotifications}>Mark all read</button></div><div className="notification-list">{data.notifications.map(n=><button key={n.id} onClick={()=>markNotification(n.id)} className={`notification-row ${n.read?'':'unread'}`}><span className="notification-dot"/><div><b>{n.title}</b><p>{n.body}</p></div><small>{n.time}</small></button>)}</div></div>}
