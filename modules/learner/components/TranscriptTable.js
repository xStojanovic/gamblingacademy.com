'use client';
import {useLearning} from '@/modules/learner/components/LearningStore';import {downloadCsv} from '@/modules/enterprise/services/csv';
export default function TranscriptTable(){const {data}=useLearning();const total=data.transcript.reduce((s,x)=>s+(x.hours||0),0);return <div className="dashboard-panel"><div className="row-between"><div><h3>Learning transcript</h3><p className="muted small">A portable record of completed learning and assessment outcomes · {total.toFixed(1)} hours recorded.</p></div><button className="button button-secondary button-small" onClick={()=>downloadCsv('opsacademy-learning-transcript.csv',data.transcript)}>Export CSV</button></div><div className="table-wrap"><table className="table"><thead><tr><th>Item</th><th>Type</th><th>Completed</th><th>Result</th><th>Hours</th></tr></thead><tbody>{data.transcript.map(x=><tr key={x.id}><td><b>{x.item}</b></td><td>{x.type}</td><td>{x.completed}</td><td>{x.result}</td><td>{x.hours}</td></tr>)}</tbody></table></div></div>}
