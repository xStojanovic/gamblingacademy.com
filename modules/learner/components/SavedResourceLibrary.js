'use client';import {resources} from '@/modules/learning/data/catalog';import {useLearning} from '@/modules/learner/components/LearningStore';import ResourceCard from '@/modules/public/components/ResourceCard';
export default function SavedResourceLibrary(){const {data}=useLearning();const saved=resources.filter(r=>data.savedResources.includes(r.slug));return <>{saved.length?<div className="grid-2">{saved.map(r=><ResourceCard key={r.slug} resource={r}/>)}</div>:<div className="empty-state"><h3>No saved resources yet</h3><p className="muted">Browse the Resource Library and save useful templates to see them here.</p></div>}</>}
