'use client';
import {useState} from 'react';import {communityPosts as seed} from '@/modules/community/data/seed';
export default function CommunityFeed(){const [posts,setPosts]=useState(seed);const [text,setText]=useState('');function add(){if(!text.trim())return;setPosts([{id:`cp-${Date.now()}`,author:'Demo Learner',role:'OpsAcademy member',topic:'General',title:text,body:'',replies:0,likes:0,time:'Now'},...posts]);setText('')}return <><div className="dashboard-panel community-compose"><h3>Professional community</h3><p className="muted small">Discuss workplace learning, processes, careers and cross-functional industry knowledge.</p><div className="row"><input value={text} onChange={e=>setText(e.target.value)} placeholder="Start a professional discussion…"/><button className="button button-small" onClick={add}>Post</button></div></div><div className="community-feed">{posts.map(p=><article className="dashboard-panel" key={p.id}><div className="row-between"><div><b>{p.author}</b><div className="muted small">{p.role}</div></div><span className="pill">{p.topic}</span></div><h3>{p.title}</h3>{p.body&&<p className="muted">{p.body}</p>}<div className="row muted small"><span>{p.replies} replies</span><span>{p.likes} useful</span><span>{p.time}</span></div></article>)}</div></>}
