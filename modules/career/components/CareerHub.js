import {renderText} from '@/modules/shared/utils/renderText';
import Link from 'next/link';import {careerRoles,jobPosts} from '@/modules/career/data/seed';
export default function CareerHub({showJobs=true}){return <><div className="grid-2">{careerRoles.map(r=><div className="card" key={r.slug}><div className="row-between"><span className="pill">{r.department}</span><span className="muted small">{r.level}</span></div><h3>{r.title}</h3><p className="muted">Recommended learning path: <b>{r.path}</b></p><div className="skill-cloud">{r.skills.map(s=><span key={renderText(s)}>{renderText(s)}</span>)}</div><Link className="text-link" href="/learning-paths">Explore learning paths →</Link></div>)}</div>{showJobs&&<div className="dashboard-panel" style={{marginTop:24}}><div className="row-between"><div><h3>Career opportunities</h3><p className="muted small">Demo job-board structure. Production listings should be verified before publishing.</p></div></div>{jobPosts.map(j=><div className="lesson-resource-row" key={j.id}><div><b>{j.title}</b><div className="muted small">{j.company} · {j.location}</div></div><span>{j.type}</span><span className="status">{j.status}</span></div>)}</div>}</>}
