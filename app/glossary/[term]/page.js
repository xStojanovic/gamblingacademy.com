import Link from 'next/link';
import {notFound} from 'next/navigation';
import {glossary} from '@/modules/learning/data/catalog';
export default async function GlossaryTermPage({params}){const {term}=await params; const item=glossary.find(([name])=>name.toLowerCase()===decodeURIComponent(term).toLowerCase()); if(!item)notFound(); return <><section className="page-hero"><div className="container narrow-wide"><div className="eyebrow">operations Glossary</div><h1>{item[0]}</h1><p className="lead">{item[1]}</p><div className="hero-actions"><Link className="button button-secondary" href="/knowledge">← Knowledge Hub</Link></div></div></section><section className="section"><div className="container narrow-wide"><div className="card"><h2>How to use this term at work</h2><p className="lead" style={{fontSize:18}}>Treat glossary definitions as a shared starting point. Internal definitions, contracts and regulatory contexts may differ, especially for commercial metrics. Use your company’s approved definition when making operational or financial decisions.</p></div></div></section></>}
