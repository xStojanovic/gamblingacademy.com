'use client';
import {use,useState} from 'react';import Link from 'next/link';
export default function InvitePage({params}){const {token}=use(params);const [form,setForm]=useState({fullName:'',password:''});const [state,setState]=useState({loading:false,error:'',done:false});async function accept(){setState({loading:true,error:'',done:false});try{const res=await fetch('/api/company/invitations/accept',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({token,...form})});const json=await res.json();if(!res.ok)throw new Error(json.error||'Could not accept invitation');setState({loading:false,error:'',done:true})}catch(e){setState({loading:false,error:e.message,done:false})}}
 return <section className="section"><div className="container narrow">{state.done?<div className="dashboard-panel center"><div className="eyebrow">Invitation accepted</div><h1>Your academy account is ready.</h1><p className="muted">Sign in with your email and the password you just created.</p><Link className="button" href="/login">Go to login</Link></div>:<div className="dashboard-panel"><div className="eyebrow">Company invitation</div><h1>Join OpsAcademy</h1><p className="muted">Complete your account to access learning assigned by your company.</p><label className="field"><span>Full name</span><input value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})}/></label><label className="field"><span>Create password</span><input type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></label>{state.error&&<div className="feedback">{state.error}</div>}<button className="button button-block" onClick={accept} disabled={state.loading||!form.password}>{state.loading?'Creating account…':'Accept invitation'}</button></div>}</div></section>}
