import {NextResponse} from 'next/server';import {env} from '@/modules/infrastructure/config/env';
export async function POST(req){const {customerId}=await req.json();if(!env.stripeSecretKey)return NextResponse.json({ok:true,demo:true,message:'Billing portal adapter is ready; configure Stripe credentials.'});if(!customerId)return NextResponse.json({error:'customerId is required'},{status:400});const p=new URLSearchParams({customer:customerId,return_url:`${env.appUrl}/company/billing`});const r=await fetch('https://api.stripe.com/v1/billing_portal/sessions',{method:'POST',headers:{Authorization:`Bearer ${env.stripeSecretKey}`,'Content-Type':'application/x-www-form-urlencoded'},body:p});const data=await r.json();return r.ok?NextResponse.json({url:data.url}):NextResponse.json({error:data.error?.message||'Stripe error'},{status:400})}
