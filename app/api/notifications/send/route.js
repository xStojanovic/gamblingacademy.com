import {NextResponse} from 'next/server';import {env} from '@/modules/infrastructure/config/env';
const subjects={welcome:'Welcome to OpsAcademy',assigned:'A new learning assignment is ready',certificate:'Your OpsAcademy credential is ready',reminder:'Continue your learning'};
export async function POST(req){const {template='welcome',to,variables={}}=await req.json();if(!to)return NextResponse.json({error:'Recipient required'},{status:400});if(!env.resendApiKey)return NextResponse.json({ok:true,demo:true,preview:{to,subject:subjects[template]||template,variables}});const r=await fetch('https://api.resend.com/emails',{method:'POST',headers:{Authorization:`Bearer ${env.resendApiKey}`,'Content-Type':'application/json'},body:JSON.stringify({from:process.env.EMAIL_FROM||'OpsAcademy <academy@example.com>',to:[to],subject:subjects[template]||template,html:`<h2>${subjects[template]||template}</h2><p>${variables.message||'You have a new OpsAcademy update.'}</p>`})});const data=await r.json();return NextResponse.json(data,{status:r.ok?200:400})}
