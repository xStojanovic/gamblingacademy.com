import {NextResponse} from 'next/server';import {requireServerRole} from '@/modules/auth/services/serverAuth';import {env} from '@/modules/infrastructure/config/env';import {dbInsert,dbSelect} from '@/modules/infrastructure/services/supabaseRest';
export async function GET(){const auth=await requireServerRole(['academy_admin']);if(!auth.ok)return NextResponse.json({error:auth.error},{status:auth.status});if(env.demoMode)return NextResponse.json({ok:true,demo:true,data:[]});const r=await dbSelect('scheduled_reports','select=*&order=created_at.desc');return NextResponse.json(r,{status:r.ok?200:500})}
export async function POST(request){const auth=await requireServerRole(['academy_admin']);if(!auth.ok)return NextResponse.json({error:auth.error},{status:auth.status});const b=await request.json();if(!b.name||!b.cadence)return NextResponse.json({error:'Name and cadence required'},{status:400});if(env.demoMode)return NextResponse.json({ok:true,demo:true,data:b});const r=await dbInsert('scheduled_reports',{owner_user_id:auth.session.user.id,name:b.name,report_type:b.reportType||'executive',recipients:b.recipients||[],cadence:b.cadence,format:b.format||'pdf_csv',filters:b.filters||{}});return NextResponse.json(r,{status:r.ok?200:500})}
