import Link from 'next/link';
import {glossary,knowledgeArticles} from '@/modules/learning/data/catalog';
export const metadata={title:'Knowledge Hub — OpsAcademy'};
export default function KnowledgePage(){return <><section className="page-hero"><div className="container"><div className="eyebrow">Knowledge Hub</div><h1>Learn the language and structure of operations.</h1><p className="lead">Free professional explainers for people entering the industry and teams building shared terminology.</p></div></section><section className="section"><div className="container"><div className="section-head"><div className="eyebrow">Latest explainers</div><h2>Start with practical context.</h2></div><div className="grid-3">{knowledgeArticles.map(a=><Link className="card article-card" key={a.slug} href={`/knowledge/${a.slug}`}><div className="course-topline"><span className="pill">{a.category}</span><span className="muted small">{a.read}</span></div><h3>{a.title}</h3><p className="muted">{a.summary}</p><span className="text-link">Read article →</span></Link>)}</div></div></section><section className="section section-border"><div className="container"><div className="section-head"><div className="eyebrow">Glossary</div><h2>Common industry terms.</h2><p className="lead">Use these as orientation, then confirm company-specific definitions where required.</p></div>{glossary.map(([term,definition])=><Link className="glossary-row" key={term} href={`/glossary/${term.toLowerCase()}`}><strong>{term}</strong><span className="muted">{definition}</span></Link>)}</div></section></>}
