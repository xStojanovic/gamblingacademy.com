'use client';import Link from 'next/link';import DashboardNav from '@/modules/learner/components/DashboardNav';import WorkspaceHeader from '@/modules/shared/components/WorkspaceHeader';import {useLearning} from '@/modules/learner/components/LearningStore';
export default function DashboardCertificatesPage(){const {data}=useLearning();return <section className="section workspace-section"><div className="container"><div className="dashboard-shell"><DashboardNav active="certificates"/><div className="dashboard-content"><WorkspaceHeader eyebrow="Learner / Credentials" title="Certificates" description="Your verified record of completed OpsAcademy professional programs." actions={<Link className="button button-secondary button-small" href="/certifications/verify">Verify credential</Link>}/>{data.certificates.map((c,i)=><div className="certificate-card" key={c.id}><div className="certificate-mark">GO</div><div><div className="eyebrow">OpsAcademy Professional Certificate</div><h2>{c.program}</h2><p>This certifies that <b>{data.profile.name}</b> completed the structured learning program and passed the final assessment.</p><div className="certificate-meta"><span>Credential <b>{c.id}</b></span><span>Score <b>{c.score}</b></span><span>Issued <b>{c.issued}</b></span></div><div className="hero-actions"><Link className="button button-secondary button-small" href={`/credential/${c.id}`}>Public credential →</Link></div></div></div>)}<div className="dashboard-panel"><h3>Credential standards</h3><p className="muted">Certificates represent completion of OpsAcademy professional learning programs and successful assessment. They are not regulator-issued licences or governmental accreditation.</p></div></div></div></div></section>}
